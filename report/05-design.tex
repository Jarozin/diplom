% Разработать метод поддержки принятия решений в рамках выполнения пошаговой инструкции, заданной в графовом формате.
% Описать основные особенности предлагаемого метода.
% Сформулировать ограничения предметной области.  
% Изложить ключевые этапы метода в виде функциональной модели и схем алгоритмов.
% Выделить структуры данных, необходимые этому методу.

% NOTE:
% В конструкторском разделе описывается разрабатываемый метод
% В случае если в бакалаврском проекте разрабатывается новый метод или алгоритм, необходимо подробно изложить их суть, привести все необходимые для их реализации математические выкладки, обосновать последовательность этапов выполнения
% При этом для каждого этапа следует выделить необходимые исходные данные и получаемые результаты
% Для описания метода или алгоритма - Схема алгоритма
% В конце описания разработанного алгоритма должны быть приведены выбранные способы тестирования и сами тесты
% Перед формированием тестовых наборов данных целесообразно указать выделенные классы эквивалентности
% (тут же могут быть приведены выкладки по теоретическим рассчетам требуемой памяти и эффективности алгоритма; эти результаты могут быть использованы для обоснования правильности выбора метода из уже имеющихся альтернативных вариантов)
% Также должно быть приведено описание структуры разрабатываемого ПО, оно включает в себя:
% - описание общей структуры - определение основных частей (компонентов) и их взаимосвязей по управлению и по данным
% - декомпозицию компонентов и построение структурных иерархий
% - проектирование компонентов
% Для графического представления такого описания (если есть необходимость), следует использовать IDEF0 с декомпозицией исходной задачи на несколько уровней

% Рек. Объем 25-30 страниц
% В данном разделе будут приведены требования и ограничения разрабатываемого метода, будут описаны основные этапы разрабатываемого метода, сценарии тестирования, классы эквивалентности тестов, а также структура разрабатываемого ПО.

% \section{Требования и ограничения метода}

% Метод поддержки принятия решений в рамках выполнения пошаговой инструкции, заданной в графовом формате должен:
% \begin{enumerate}
%     \item работать с описаниями инструкций, заданных в описанном графовом формате;
%     \item хранить текущий прогресс пользователя при выполнении инструкции;
%     \item уметь выбирать лучший вариант действия при наличии нескольких вариантов следующих действий;
%     \item выполнять переход по графу в соответствие с действиями пользователя;
%     \item предоставлять пользователю рекомендации при объявлении пользователю выбранного варианта следующих действий;
%     \item из возможных вариантов рекомендаций выбирать наиболее подходящие;
%     \item для корректировки весов давать возможность экспертам корректировать алгоритм рекомендаций.
% \end{enumerate}

% % Возможно дебаг экспертом стоит оставить на позжую часть работы
% % Также можно разделить алгоритмы рекомендации и движение по графу

% \section{Описание разрабатываемого метода}

% Поставленную задачу можно разбить на следующие этапы:
% \begin{enumerate}
%     \item планирование следующих действий;
%     \item формирование рекомендаций для оператора;
%     \item вывод следующего действия и соответствующих рекомендаций оператору;
%     \item обработка результатов действия пользователя.
% \end{enumerate}

% Алгоритм формирования рекомендаций будет описан в данном разделе далее.

% Основные этапы разрабатываемого метода представлены на IDEF0 диаграмме первого уровня (см. Рисунок ).

% Рисунок

% \section{Планирование следующих действий}

% Исходные данные --- инструкция в графовом формате и вершина графа, описывающая текущий этап выполнения инструкции.
% Получаемый результат --- предлагаемая следующая вершина графа и переход, описывающий действие, необходимое для достижения нужного состояния.

% На рисунке представлена IDEF0 диаграмма планирования следующих действий.

% % Выбор кратчайшего пути с помощью чего-то вроде волнового алгоритма 
% % или
% % Предварительная разметка экспертами главного пути
% % Можно попытаться скрыть выбор следующего действия под словом планирование


% % Возможно стоит поменять формат графа
% Текущее положение описывается следующими сущностями:
% \begin{enumerate}
%     \item possible\_actions --- массив, $i$-й элемент которого описывает $i$-ый из возможных из текущего состояния переходов в $i$-ое состояние;
%     \item linked\_states --- массив, $i$-й элемент которого описывает $i$-ый все состояния, в которые возможны переходы;
%     \item actor --- субъект, выполняющий действие;
%     \item object --- объект, над которым выполняется действие.
% \end{enumerate}

% % Если мы может использовать один переход для нескольких состояний/сущностей, где-то будет необходимо хранить актуальные начало-конец, а не только возможные. 
% % Если для каждого объекта они свои, то скорее всего придется тут. 
% % Если они общие (все начинают ненарезанные -> все становятся нарезанными), то такое необходимости нет, объекты хранятся на уровне выше.
% Каждое переход описывается следующими данными:
% \begin{enumerate}
%     \item text --- текст, описывающий действие пользователя;
%     \item tags --- массив, $i$-й элемент которого описывает тэг, описывающий данное действие;
%     \item starting\_state --- состояние, в котором изначально может находиться объект действия;
%     \item ending\_state --- состояние, в котором конечно может находится объект.
% \end{enumerate}

% Tag описывается следующими данными:
% \begin{enumerate}
%     \item name --- текстовое поле, описывает название;
%     \item coefficients --- массив, $i$-й элемент которого является кортежем типа $(сriteria\_name, weight, C)$, где criteria\_name --- имя критерия, weight --- значение типа float от 0 до 1, описывающее важность критерия для данного тэга;
%     \item recommendation --- текстовое поле, описывающее рекомендацию, которую необходимо дать оператору при присутствии tag у действияю.
% \end{enumerate}

\chapter{Конструкторский раздел}

\section{Постановка задачи} 
Целью разработки является создание метода поддержки принятия решений при выполнении пошаговой инструкции, представленной в графовом формате. 
Инструкция описывает последовательность действий, которые переводят систему из начального состояния в конечное.
Для повышения качества выполнения инструкции необходимо формировать рекомендации, учитывающие контекст и обусловленные прагматикой задачи в заданном домене критерии, например, безопасность, полезность. 
Метод должен обеспечивать следующее:
\begin{itemize}
    \item использование формального описания инструкции в описанном графовом формате;
    \item автоматическое извлечение контекстных меток для каждого действия на основе названия и описания;
    \item возможность использования экспертных описаний внештатных ситуаций в некотором контексте и графов их инструкций, описывающих порядок действий при возникновении таких ситуаций;
    \item возможность использования заранее заготовленных экспертом рекомендаций, их соотнесения с контекстными метками и их оценивания как альтернатив; 
    \item сопровождение пользователя в процессе пошагового прохождения инструкции --- формирование и отображение рекомендаций (поддержка принятия решений оператором);
    \item выдача лучших рекомендаций согласно ранжированию с использованием базового или модифицированного метода анализа иерархий (МАИ) с возможностью настройки весов критериев (см. п.~\ref{MAI});
    % TODO: перепроверить этот момент
    \item возможность учитывать уровень или частные дескрипторы экспертизы оператора при оценке рекомендаций экспертом для адаптации ранжирования рекомендаций к уровням экспертизы конкретных операторов;
    \item %предоставление оператору
    динамическое добавление в граф инструкции описанных экспертами контекстно-актуальных инструкций при возникновении внештатных ситуаций по ходу пошагового выполнения инструкции оператором;
    \item возможность внесения экспертом корректировок приоритетов на материале выполнения инструкций в режиме отладки.  
\end{itemize}

\section{Основные особенности предлагаемого метода}

Предложенный метод базируется на следующем.

\begin{enumerate}
    \item \textbf{Графовое представление инструкции}. 
    Инструкция описывается ориентированным графом без циклов, вершины которого соответствуют состояниям, объектам и действиям, а дуги --- связям между ними.
    Состояния содержат описание свойств объектов (атрибутов) в данный момент. 
    Это позволяет отслеживать динамику изменения свойств объектов.
    
    \item \textbf{Интерактивный пошаговый режим}.
    Оператор последовательно выбирает доступные действия, определённые в выбранном графе пошаговой инструкции. 
    Перед подтверждением выбора ему показываются $N$ наиболее подходящих согласно МАИ рекомендации с указанием их оценок.
    Все выполненные шаги сохраняются в историю вместе с показанными рекомендациями.

    \item \textbf{Автоматическое присвоение меток действиям}. 
    Для каждого действия на основе его имени и описания по ключевым словам определяются метки из предопределённого набора. 
    Метки служат для определения контекста, на основе которого отбираются рассматриваемые рекомендации и актуальные внештатные ситуации.


    \item \textbf{Ранжирование рекомендаций на основе МАИ}.
    Используется модифицированный алгоритм МАИ: веса критериев вычисляются полным методом анализа иерархий (матрица попарных сравнений, геометрическое среднее, проверка согласованности), а оценки альтернатив (рекомендаций) задаются непосредственно экспертом. 
    Данный выбор обусловлен предполагаемой неизменностью количества критериев оценки для заданного домена, это позволяет задать полную матрицу попарных сравнений, и предполагаемой расшираемостью множества рекомендаций, в результате чего в случае полного парного сравнения каждая новая рекомендация будет требовать больше сравнений, чем предыдущая, усложняя добавление новых рекомендаций.
    %Итоговые оценки нормализуются, что позволяет интерпретировать их как вероятности выбора.

    \item \textbf{Обработка аварийных ситуаций}.
    При получении от оператора информации о неудаче выполнения действия (равно как и невыполнении оного), система предлагает контекстно-зависимые аварийные сценарии.
    Каждый аварийный сценарий представляет собой отдельный граф, описанный экспертом, который накладывается на основной граф.
    Данный граф имеет такое же формат описания, как и основной граф.
    Для обеспечения их контекстной актуальности, каждая аварийная ситуация содержит метки, указывающие на возможность возникновения этих ситуаций при выполнении текущего действия.
    Его стартовая вершина ставится после текущей вершины графа.
    Так как в результате некоторых аварийных ситуаций возврат к выполнению изначальной инструкции считается невозможным, каждая аварийная ситуация также описывается флагом возможности возврата в исходный граф.
    По достижении конечной вершины аварийного сценария, если флаг укзывается на возможность возврата, происходит возврат в изначальный граф и продолжение выполнения с той же вершины, с которой был произведен переход на аварийный граф, иначе выполнение инструкции завершается.

    \item \textbf{Режим экспертной отладки}. 
    Эксперт может в процессе отладки изменять веса критериев и оценки рекомендаций по критериям, наблюдая за пересчётом итоговых оценок и ранжированием. 
    Эксперт также может сохранить лог внесённых изменений и готовые к использованию новые изменённые файлы конфигурации. 
    % логируются и могут быть сохранены в отдельные файлы конфигурации и полные модифицированные подготовленные данные \textcolor{red}{Наверное, можно где-то в 2 части сказать, что такое "Подготовленные данные" или "комплект...", "результаты параметризации", "используемые... характеристики"...}.

    %Данный подграф имеет свою отдельную цель, которая назначается текущей и к которой надлежит привести оператора. КАК ПОТОМ? ВЕРНУТЬСЯ К БАЗОВОМУ ПУТИ. Если авария устранена и можно вернуться, надо как-то сделать переход в прежний (пред.уровня) граф с возвратом к прежней цели и назначением новой текущей вершины согласно запомненной в момент аварии конфигурации выполнения инструкции, а если состояние аварии является конечным и возврат невозможен, то выполнении инструкции завершается в конечной вершине добавленного подграфа.
\end{enumerate}

\section{Ограничения предметной области}

Предлагаемый метод применим при соблюдении следующих ограничений.

\begin{itemize}
    % TODO: добавить ссылку на аналит(+)
    \item Инструкция должна быть задана в виде ориентированного графа без циклов (допускаются простые циклы, но они могут привести к бесконечному выполнению, что должно контролироваться дополнительно) (см. п.~\ref{graph_format}).
    \item Эксперт может задать оценки рекомендаций по заданным критериям в шкале от 0 до 1. 
    Для новых инструкций требуется предварительная настройка меток и оценок, иначе рекомендации могут отсутствовать.
    \item Аварийные сценарии должны быть предварительно описаны в виде отдельных графов и снабжены метками для сопоставления с действиями основного графа.
\end{itemize}

\section{Функциональная модель метода}
В соответствии с методологией IDEF0, процесс поддержки принятия решений можно представить как последовательность функциональных блоков.

Первый уровень приближения представлен на рисунке \ref{fig:idef0}.
\begin{figure}[H]
	\centering
\includegraphics[width=0.9\linewidth]{inc/img/idef1.pdf}
	\caption{Формализация задачи поддержки пользователя при выполнении инструкции (A0)}
    \label{fig:idef0}
\end{figure}

Следующий уровень приближения блока A0 представлен на рисунке \ref{fig:idef0_x}.
\begin{figure}[H]
	\centering
\includegraphics[width=0.9\linewidth]{inc/img/idef0_x.pdf}
	\caption{Формализация задачи поддержки пользователя при выполнении инструкции (A1, A2)}
    \label{fig:idef0_x}
\end{figure}


Следующий уровень приближения блока A1 представлен на рисунке \ref{fig:idef1_x}.
\begin{figure}[H]
	\centering
\includegraphics[width=0.9\linewidth]{inc/img/idef_1x.pdf}
	\caption{Формализация задачи поддержки пользователя при выполнении инструкции (детализация A1)}
    \label{fig:idef1_x}
\end{figure}


Следующий уровень приближения представлен A2 на рисунке \ref{fig:idef2_x}.
\begin{landscape}
\begin{figure}[H]
	\centering
\includegraphics[width=0.9\linewidth]{inc/img/idef2_x.pdf}
	\caption{Формализация задачи поддержки пользователя при выполнении инструкции (детализация A2)}
    \label{fig:idef2_x}
\end{figure}
\end{landscape}


\section{Используемые структуры данных}

Для реализации метода определены следующие основные структуры данных.

\textbf{Структура данных Object} содержит следующие данные.
\begin{enumerate}
    \item name --- имя объекта (строка).
    \item obj\_type --- тип сущности: object (объект) или subject (субъект).
    \item properties --- словарь динамических свойств объекта, значения могут быть строкой, числом, булеан.
\end{enumerate}

\textbf{Структура данных State} содержит следующие данные.
\begin{enumerate}
    \item id --- уникальный идентификатор состояния;
    \item name --- название состояния;
    \item description --- текстовое описание состояния;
    \item state\_type --- тип состояния: начальное, промежуточное, конечное, ошибка;
    \item objects\_state --- словарь, где ключ --- имя объекта, значение --- словарь свойств этого объекта в данном состоянии.
\end{enumerate}

\textbf{Структура данных Action} содержит следующие данные.
\begin{enumerate}
    \item id --- уникальный идентификатор действия;
    \item name --- название действия;
    \item description --- текстовое описание действия;
    \item required\_objects --- множество имён объектов, необходимых для выполнения действия;
    \item produced\_objects --- множество имён объектов, создаваемых в результате действия;
    \item consumed\_objects --- множество имён объектов, потребляемых при выполнении действия.
\end{enumerate}

\textbf{Структура данных Transition} содержит следующие данные.
\begin{enumerate}
    \item from\_state --- идентификатор исходного состояния;
    \item action\_id --- идентификатор действия;
    \item to\_state --- идентификатор состояния, в которое осуществляется переход.
\end{enumerate}

\textbf{Структура данных Label} содержит следующие данные.
\begin{enumerate}
    \item name --- имя метки;
    \item keywords --- список ключевых слов, по которым действие сопоставляется с меткой;
    \item recommendations --- список рекомендаций, каждая из которых представляет собой словарь с полями text (текст рекомендации) и scores (оценки по критериям).
\end{enumerate}

\textbf{Структура данных LabelManager} содержит следующие данные.
\begin{enumerate}
    \item labels --- список загруженных меток;
    \item action\_labels\_cache --- кэш, сопоставляющий идентификатор действия с соответствующими ему метками;
    \item ranker --- объект, реализующий ранжирование рекомендаций на основе метода анализа иерархий.
\end{enumerate}

\textbf{Структура данных AHPMethod} содержит следующие данные.
\begin{enumerate}
    \item criteria\_names --- список названий критериев;
    \item pairwise\_matrix --- матрица попарных сравнений критериев;
    \item weights --- вычисленные веса критериев;
    \item consistency\_ratio --- отношение согласованности матрицы;
    \item is\_consistent --- флаг, указывающий, является ли матрица согласованной.
\end{enumerate}

\textbf{Структура данных EmergencyScenario} содержит следующие данные.
\begin{enumerate}
    \item id --- идентификатор сценария;
    \item description --- текстовое описание аварийной ситуации;
    \item file --- имя файла, содержащего граф аварийного сценария;
    \item return\_to\_original --- флаг, определяющий, следует ли возвращаться к основному сценарию после завершения аварийного;
    \item tags --- набор меток (ключевых слов), используемых для сопоставления с действиями.
\end{enumerate}

\textbf{Структура данных EmergencyHandler} содержит следующие данные.
\begin{enumerate}
    \item scenarios\_config --- словарь, загруженный из конфигурационного файла аварийных сценариев;
    \item recursion\_depth --- текущая глубина рекурсии при вложенных аварийных сценариях;
    \item max\_depth --- максимально допустимая глубина рекурсии.
\end{enumerate}

\textbf{Структура данных HistoryStep} содержит следующие данные.
\begin{enumerate}
    \item from\_state --- идентификатор исходного состояния;
    \item from\_state\_name --- название исходного состояния;
    \item action\_id --- идентификатор выполненного действия;
    \item action\_name --- название выполненного действия;
    \item to\_state --- идентификатор состояния после выполнения действия;
    \item to\_state\_name --- название состояния после выполнения действия;
    \item recommendations --- список рекомендаций, показанных пользователю перед выполнением действия (каждая рекомендация содержит текст и нормализованную оценку).
\end{enumerate}
\textbf{Структура данных ModifiedAHP} содержит следующие данные.
\begin{enumerate}
\item criteria\_names --- список строк, содержащий названия всех критериев, загруженных из конфигурационного файла.

\item criteria\_weights --- словарь, где ключ --- название критерия (строка), значение --- его глобальный вес (число с плавающей точкой), вычисленный методом геометрического среднего на основе матрицы попарных сравнений критериев.

\item root\_elements --- список элементов корневой группы (строки). 
Каждый элемент может быть либо именем группы, либо именем отдельной альтернативы, не входящей ни в одну группу. 
\item elements\_data --- словарь, отображающий имя группы (строка) на список её элементов. 
Дочерние элементы могут быть следующими:
\begin{itemize}
\item имена вложенных групп (строки);
\item имена конечных альтернатив (строки).
\end{itemize}

\item comparisons\_by\_criterion --- трёхуровневый вложенный словарь вида:
\begin{verbatim}
[критерий][группа] -> {(элемент1, элемент2): значение},
\end{verbatim}
где:
\begin{itemize}
\item первый ключ --- название критерия (строка);
\item второй ключ --- имя группы (группы или служебное имя root);
\item значение --- словарь, в котором ключами являются пары строк (элемент1, элемент2), а значениями — интенсивности превосходства по шкале Саати (числа от 1 до 9 или обратные им).
\end{itemize}
Эта структура хранит все парные сравнения, заданные экспертом для каждой группы и каждого критерия.

\item total\_comparisons --- целое число, равное общему количеству парных сравнений, загруженных из конфигурационного файла. Суммируются сравнения критериев, сравнения элементов внутри всех групп для всех критериев.
\end{enumerate}
\section{Алгоритмы}
\subsection{Алгоритм выбора уровня экспертизы пользователя}

Вход: файл с размеченными экспертом весами критериев для описанных уровней экспертизы.

Выход: набор весов критериев, используемый для расчёта рекомендаций.

Шаги алгоритма описаны далее.
\begin{enumerate}
    \item Вывести на экран меню выбора уровня экспертности:
        \begin{itemize}
            \item 1 — «Новичок» (приоритет безопасности);
            \item 2 — «Опытный» (сбалансированный подход);
            \item 3 — «Эксперт» (приоритет критичности и полезности).
        \end{itemize}
    \item Считать ввод пользователя и преобразовать его в целое число.
    \item Сопоставить введённое число с именем профиля:
        \begin{itemize}
            \item 1 → \texttt{новичок}
            \item 2 → \texttt{опытный}
            \item 3 → \texttt{эксперт}
            \item любое другое значение → \texttt{опытный} (по умолчанию).
        \end{itemize}
    \item Загрузить из файла парные сравнения критериев, соответствующие выбранному профилю.
\end{enumerate}

\subsection{Алгоритм обработки аварийных ситуаций}

Вход: действие \(a\), которое оператор отказался выполнять, текущий контекст (метки действия).

Выход: флаг необходимости возврата к основному сценарию.

Шаги алгоритма описаны далее.
\begin{enumerate}
    \item Получить ключевые слова из всех меток действия \(K_a\).
    \item Для каждого аварийного сценария \(e\) из конфигурации:
        \begin{itemize}
            \item Если множество меток сценария \(T_e\) пересекается с \(K_a\), добавить сценарий в список доступных.
        \end{itemize}
    \item Предложить оператору выбрать сценарий из списка.
    \item Если выбор сделан:
        \begin{itemize}
            \item Загрузить граф сценария из файла.
            \item Рекурсивно запустить его выполнение (с ограничением глубины).
            \item По завершении вернуть флаг \(return\_to\_original\) из конфигурации сценария.
        \end{itemize}
    \item Иначе продолжить основной сценарий.
\end{enumerate}

\subsection{Алгоритм пошагового выполнения}

Вход: граф инструкции \(G\), начальное состояние \(s_0\), количество необходимых лучших рекомендаций $n$.

Выход: история выполнения \(H\).

% TODO: двоеточие: точка. Возможно тут и стоит сделать схему алгоритма
Шаги алгоритма описаны далее.
\begin{enumerate}
    \item Установить текущее состояние \(s = s_0\), очистить историю.
    \item Пока не достигнуто конечное состояние и не нажата команда выхода:
        \begin{enumerate}
            \item Вывести информацию о текущем состоянии и свойствах объектов.
            \item Получить список доступных действий \(A_{\text{ok}}\) (все действия, для которых есть переход).
            \item Для каждого доступного действия:
                \begin{itemize}
                    \item найти метки, соответствующие действию;
                    \item найти все рекомендации, соответствующие метке;
                    \item выполнить ранжирование по выбранному алгоритму ранжирования;
                    \item выбрать n лучших рекомендации.
                \end{itemize}
            \item Отобразить оператору список действий с указанием их меток.
            \item При выборе оператором действия:
                \begin{itemize}
                    \item вывести n лучших рекомендации с оценками;
                    \item запросить подтверждение;
                    \item если подтверждено, выполнить действие, перейти в новое состояние \(s'\);
                    \item если не подтверждено, запустить обработку аварийной ситуации;
                    \item добавить запись в историю: \((s, a, s', \text{показанные рекомендации})\).
                \end{itemize}
        \end{enumerate}
    \item Сохранить историю в файл.
\end{enumerate}

\subsection{Алгоритм метода анализа иерархии}

Метод анализа иерархий можно разбить на шаг поиска весов критериев и шаг ранжирования результатов. 

\subsection*{Шаг вычисления весов критериев}

Вход: список критериев \(C = \{c_1,\dots,c_n\}\), матрица попарных сравнений \(A\) (размер \(n \times n\)), где \(a_{ij}\) --- степень превосходства критерия \(c_i\) над \(c_j\) по шкале Саати.

Выход: вектор весов \(w = (w_1,\dots,w_n)\), индекс согласованности \(CI\), отношение согласованности \(CR\).

Далее описана последовательность действий, предпринимаемая алгорит-
мом.
\begin{enumerate}
    \item Для каждой строки \(i\) матрицы попарных сравнений вычислить геометрическое среднее:
    \[
    g_i = \left( \prod_{j=1}^{n} a_{ij} \right)^{1/n}.
    \]
    \item Нормализовать: \(w_i = g_i / \sum_{k=1}^{n} g_k\).
    \item Вычислить вектор \(Aw\): \((Aw)_i = \sum_{j=1}^{n} a_{ij} w_j\).
    \item Найти \(\lambda_{\max} = \frac{1}{n} \sum_{i=1}^{n} \frac{(Aw)_i}{w_i}\).
    \item Индекс согласованности: \(CI = \frac{\lambda_{\max} - n}{n-1}\).
    \item Отношение согласованности: \(CR = CI / RI\), где \(RI\) --- случайный индекс для размера \(n\) (таблица Саати).
    \item Если \(CR < 0.1\), матрица считается согласованной.
\end{enumerate}

\subsection*{Шаг ранжирования рекомендаций для действия}

Вход: действие \(a\) (его название и описание), набор меток \(L\) с рекомендациями \(R_a = \{r_1,\dots,r_m\}\), веса критериев \(w_c\), количество искомых лучших рекомендаций $n$.

Выход: список рекомендаций, отсортированный по убыванию нормализованной оценки.

Шаги алгоритма описаны далее.
\begin{enumerate}
    \item Инициализировать пустой список оценок \(S\).
    \item Для каждой рекомендации \(r_i\):
    \begin{itemize}
            \item получить оценки по критериям \(s_{i,c}\) из конфигурации (если отсутствуют, использовать значение по умолчанию 0.5).
            \item вычислить интегральную оценку \(S_i = \sum_{c} w_c \cdot s_{i,c}\).
        \end{itemize}
        \item Вычислить сумму \(T = \sum_{i=1}^{m} S_i\).
        \item Если \(T > 0\), нормализовать \(\tilde{S}_i = S_i / T\); иначе \(\tilde{S}_i = 1/m\).
    \item Отсортировать рекомендации по убыванию \(\tilde{S}_i\).
    \item Вернуть n лучших элементов с их оценками.
\end{enumerate}

\section{Модифицированный алгоритм метода анализа иерархий}
\label{MAI}

\subsection*{Мотивация и постановка задачи}

Метод анализа иерархий (МАИ) требует попарного сравнения всех альтернатив по каждому критерию.
Для числа альтернатив \(m\) количество необходимых сравнений растёт квадратично: \(\frac{m(m-1)}{2}\) на один критерий.
При наличии нескольких критериев это число умножается на количество критериев \(k\). 
Для задач поддержки принятия решений с десятками или сотнями альтернатив такой объём экспертной работы становится непрактичным.

В реальных задачах часто можно объединить альтернативы в группы по некоторому признаку.
% TODO: кавычки вокруг имен классов (+)
Например, при выборе автомобиля можно выделить классы <<бюджетные>>, <<семейные>>, <<премиальные>>; внутри каждого класса сравнение происходит уже между ограниченным числом моделей.
Аналогично, в задаче ранжирования рекомендаций можно сгруппировать советы по тематике (безопасность, экономия времени, качество результата).

Цель модификации --- сократить количество требуемых попарных сравнений за счёт введения группировки альтернатив.

\subsection*{Основные принципы модифицированного МАИ}

Для описание основных принципов необходимо ввести дополнительные понятия.
\begin{enumerate}
    \item \textbf{Группой} называется непустой набор элементов. 
    \item \textbf{Элементом группы} может быть группа или альтернатива.
    \item \textbf{Корневой группой} называется группа, которая не входит ни в какую другую группу.
\end{enumerate}

Предлагаемый метод основан на следующих принципах.

\begin{enumerate}
    \item Все группы кроме корневой должны принадлежат к группе.
    \item Парные сравнения выполняются только внутри группы.
    \item Все элементы группы с более высоким приоритетом считаются важнее всех элементов группы с меньшим приоритетом.
    \item Предлагаемый метод является жадным, то есть выполняется до тех пор, пока не будет набрано необходимое количество лучших альтернатив. Это позволяет не рассчитывать веса элементов, принадлежащих к нерассматриваемым группам.
\end{enumerate}

\subsection*{Структура входных данных}

% TODO: добавить картинку

Пусть задано множество альтернатив \(A = \{a_1, a_2, \dots, a_m\}\). 
Эксперт определяет группы и для каждой группы \(G\) --- список её элементов.

Дополнительно для каждого критерия \(c\) и для каждой группы задаётся матрица попарных сравнений его элементов.

\subsection*{Алгоритм ранжирования}

Алгоритм состоит из следующих шагов.

\textbf{Шаг 1. Вычисление агрегированных весов для корневых элементов.}

% TODO: перечитать, объяснить кто такой  кто Wcroot, + сделать нормальную ссылку на МАИ(или пикчу сравнений и вычислений) (+-)
Для каждого критерия \(c\) и для корневой группы \(root\) вычисляются локальные веса ее элементов \(w_{c,\text{root}}(e)\) методом геометрического среднего на основе матрицы попарных сравнений, заданной для \(root\). 
Затем эти веса агрегируются по критериям с использованием глобальных весов критериев \(w_c\) (полученных стандартным МАИ):

\begin{equation}
    \label{equation}
    W_{\text{root}}(e) = \sum_{c} w_c \cdot w_{c,\text{root}}(e).
\end{equation}

Полученные значения определяют приоритет элементов в корневой группе.

\textbf{Шаг 2. Жадный рекурсивный отбор лучших альтернатив.}

Необходимо найти $N$ лучших альтернатив. 
Создаётся пустой список результатов \(R\). 
Для группы \(G\) с элементами \(children(G)\) выполняются следующие действия.
\begin{enumerate}
    \item Для каждого критерия \(c\) вычисляются локальные приоритеты элементов группы \(w_{c,G}(e)\) на основе матрицы попарных сравнений, заданной для группы \(G\).
    \item Вычисляются глобальные приоритеты элементов группы: 
    \[
    W_G(e) = \sum_{c} w_c \cdot w_{c,G}(e).
    \]
    \item Элементы сортируются по убыванию \(W_G(e)\).
    \item Для каждого элемента \(e\) в этом порядке, пока не набрано нужное количество альтернатив.
        \begin{itemize}
            \item Если \(e\) --- альтернатива, она добавляется в результат.
            \item Если \(e\) --- группа, рекурсивно вызывается процедура отбора из нее с передачей в качестве аргумента оставшегося необходимого количества искомых альтернатив.
        \end{itemize}
\end{enumerate}

\subsection*{Оценка количества сравнений}

Пусть имеется набор групп, в которой каждая группа \(C\) имеет \(n_C\) элементов. 
Общее число попарных сравнений для одного критерия на одном уровне составляет:

\[
\text{Comp} = \sum_{C} \frac{n_C (n_C - 1)}{2},
\]

где суммирование ведётся по всем группам (включая корневую).
Для \(k\) критериев общее количество сравнений равно \(k \cdot \text{Comp}\) в сумме с попарным сравнения критериев (обычно пренебрежимо мало по сравнению с количество попарных сравнений для альтернатив).

Тогда при $n$ начальных элементов необходимо \(\frac{n(n-1)}{2}\) попарных сравнений.
Добавление группы размера $R$ сравнимо с удалением $R$ элементов из начальной группы и добавлением вместо них одного элемента.
В таком случае для нового размера начальной группы $n_1 = (n - r + 1)$ будет необходимо \(\frac{(n - R + 1)(n - R)}{2}\) попарных сравнений.
Так как по условию метода размер группы $R$ не меньше $1$, полученное количество сравнений всегда будет не больше начального.
Если рассматривать полученную группу как элемент начального множества, который не может быть элементом добавляемых групп (так как рассматриваются только не вложенные группы), и $n1$ как новый начальный размер группы, то описанная выше логика снова применима, то есть добавление новой группы снова не приведен к увеличению количества сравнений в начальной группе.

Таким образом, количество попарных сравнений в изначальной группе после добавления внутренней группы всегда не больше начального количества попарных сравнений.
Это значит, что увеличение общего числа попарных сравнений может возникнуть только в результате вложенных групп.
Однако то же доказательство применимо и в этом случае.
Если предположить, что элементы новой вставленной группы изначально не были разбиты на группы, по той же цепочке рассуждений доказывается, что добавление новых групп во вставленную не может привести к увеличению количества попарных сравнений.

Таким образом, количество попарных сравнений по предложенному методу всегда не больше, чем в классическом МАИ.

Примеры разбиений на группы, требующие наибольшего количества попарных сравнений модифицированного метода:
\begin{enumerate}
    \item определена только корневая группа, следовательно все элементы принадлежат одной группе и попарно сравниваются;
    \item все группы состоят из одного элемента.
\end{enumerate}

Пример применения модифицированного метода: для 100 альтернатив, разбитых на 10 групп по 10 элементов, полное попарное сравнение потребовало бы \(\frac{100\cdot 99}{2} = 4950\) сравнений на критерий. 
В модифицированном методе сравнение 10 групп в корневой группе бы потребовало 45 сравнений и внутри каждой другой группы по 45 сравнений (10 групп), то есть 450 сравнений.
Таким образом в итоге бы потребовалось 495 сравнений, что в 10 раз меньше, чем в классической реализации.

Также важно упоминуть, что хотя в данном методе уменьшается количество попарных сравнений по критериям, у эксперта появляется дополнительная необходимость разбиения альтернатив на группы.

\subsection*{Сравнение с МАИ}

Сравнить классическую версию МАИ с модифицированной можно по следующим критериям.

\begin{itemize}
    % TODO: взглянуть ещё раз на это
    \item \textbf{Непосредственность оценок эксперта}: классический МАИ даёт точные глобальные приоритеты, основанные на всех попарных сравнениях, позволяя экспертам более точно определить отношение между двумя альтернативами по заданным критериям.
    Модифицированный метод заменяет часть попарных сравнений между альтернативами, принадлежащими разным группам, попарными сравнениям групп, которые едины для нескольких элементов.
    В результате множество элементов будет иметь одинаковый относительный приоритет по сравнению с другими альтернативами, даже если бы эксперт пожелал иначе в случае отстутствия групп.
    % TODO: большинстве: страшное слово(+-)
    \item \textbf{Трудоёмкость для эксперта}: модифицированный метод требует меньшего числа сравнений во большинстве случаев, что снижает необходимое количество работы для эксперта;
    % TODO:  гибкость кого(+)
    \item \textbf{Упрощение описания иерархических структур альтернатив}: модифицированный метод позволяет встраивать уже имеющиеся классификации альтернатив.
\end{itemize}

\subsection*{Выводы}

Предложенная модификация МАИ в большинстве случаев позволяет уменьшить количество необходимых попарных сравнений за счёт введения группировки альтернатив.
Метод сохраняет основные преимущества классического МАИ. 
Он особенно эффективен в задачах с большим числом альтернатив, которые естественным образом образуют группы.

\section{Описание разрабатываемого ПО}
\subsection{Функциональные требования}

Разрабатываемое ПО должно отвечать следующим функциональными требованиям:
\begin{itemize}
    \item должна быть возможность выполнения в интерактивном пошаговом режиме инструкции оператором со вводом результатов действий и выводом рекомендаций;
    \item должен быть реализован режим отладки экспертом процесса выдачи рекомендаций, в котором должна быть возможность изменений локальных приоритетов, используемых методом анализа иерархий, и возможность сохранения лога изменений и новых приоритетов в отдельные файлы;% по материалам отладки процесса выдачи рекомендаций;
    \item должна быть возможность выбора экспертизы пользователя;
    \item должна быть возможность обработки контекстно-зависимых аварийных ситуаций с помощью зараннее описанных экспертами сценариев, в том числе с динамическим обновлением состояния графа.
\end{itemize}
    % TODO: стоит это вынести как требования к ПО
% \item пригодность к реализации в виде ПО, позволяещего выполнение в интерактивном пошаговом режиме инструкции оператором со вводом результатов действий и выводом рекомендаций;
% \item режим отладки процесса выдачи рекомендаций для эксперта, в котором он сможет изменить \textcolor{red}{относительные приоритеты попарного сравнения альтернатив (ОППСА) <тут будет аббревиатура>} и оценки рекомендаций (альтернатив);% по материалам отладки процесса выдачи рекомендаций;
%\item обработку контекстно-зависимых аварийных ситуаций с помощью зараннее описанных экспертами сценариев, в том числе с динамическим обновлением состояния графа.

\subsection{Тестирование метода}

\subsection*{Классы эквивалентности для тестирования}

Тестовые наборы данных для проверки метода формируются на основе описанных далее классов эквивалентности.

\begin{itemize}
    \item \textbf{Структура графа}: линейная последовательность (отсутствие ветвлений); граф с ветвлениями; граф с циклами; граф с недостижимыми состояниями.
    \item \textbf{Наличие меток и оценок}: действие имеет полный набор меток; действие не имеет меток (рекомендации отсутствуют); действие имеет метки, но некоторые оценки по критериям не заданы (используются значения по умолчанию).
    \item \textbf{Количество рекомендаций}.
    \item \textbf{Весовая матрица МАИ}: согласованная матрица; несогласованная матрица (CR > 0.1).
    \item \textbf{Аварийные сценарии}: сценарий доступен по меткам; сценарий недоступен; рекурсивный вызов аварийного сценария.
\end{itemize}

\subsection*{Функциональные тесты}

Каждое выполнение теста включает следующие шаги:

\begin{itemize}
    \item загрузку инструкции и конфигурации меток/критериев;
    \item запуск пошагового режима, выполнение всех действий;
    \item проверку, что для каждого действия система предложила рекомендации;
    \item проверку, что нормализованные оценки рекомендаций в сумме дают 1;
    \item проверку, что после выполнения история содержит правильные переходы;
    \item в режиме отладки экспертом --- изменение весов и оценок, проверку немедленного пересчёта ранжирования;
    \item проверку обработки аварийных ситуаций: отказ от действия, выбор сценария, выполнение вложенного графа, возврат к основному.
\end{itemize}
В таблицах~\ref{tab:tests1},~\ref{tab:tests2},~\ref{tab:tests3} приведены тесты, покрывающие описанные классы эквивалентности.
\begin{table}[H]
\centering
\caption{Тестирование по классам эквивалентности часть 1}
\label{tab:tests1}
\begin{tabular}{|p{0.25\textwidth}|p{0.35\textwidth}|p{0.30\textwidth}|}
\hline
\textbf{Класс эквивалентности} & \textbf{Характеристика входных данных} & \textbf{Ожидаемый результат} \\
\hline
\multicolumn{3}{|c|}{\textbf{Структура графа}} \\
\hline
Линейная последовательность & Граф, в котором из каждого состояния есть ровно одно действие, ведущее в следующее & Все действия доступны по порядку, после последнего достигнуто конечное состояние. \\
\hline
Граф с ветвлениями & Граф, в котором из одного состояния есть несколько альтернативных действий, ведущих в разные состояния & Пользователь может выбрать любое действие; все пути ведут в конечное состояние. \\
\hline
Граф с циклами & Граф, содержащий переход из состояния A в B и из B обратно в A & Сообщение об ошибке\\
\hline
Недостижимые состояния & Граф с состоянием, не имеющим входящих переходов & Состояние недостежимо \\
\hline
\multicolumn{3}{|c|}{\textbf{Наличие меток и оценок}} \\
\hline
Полный набор меток & Действие, чьё имя и описание содержат ключевые слова из нескольких предопределённых меток & Пользователю показываются рекомендации из всех соответствующих меток. \\
\hline
Отсутствие меток & Действие, не содержащее ключевых слов ни одной из меток & Рекомендации отсутствуют, выводится сообщение об отсутствии. \\
\hline
Частично заданные оценки & Действие с меткой, у которой для некоторых критериев отсутствуют оценки в рекомендациях & Для отсутствующих оценок подставляется значение по умолчанию (0.5), итоговая оценка вычислена корректно. \\
\hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Тестирование по классам эквивалентности часть 2}
\label{tab:tests2}
\begin{tabular}{|p{0.25\textwidth}|p{0.35\textwidth}|p{0.30\textwidth}|}
\hline
\textbf{Класс эквивалентности} & \textbf{Характеристика входных данных} & \textbf{Ожидаемый результат} \\
\hline
\multicolumn{3}{|c|}{\textbf{Количество рекомендаций}} \\
\hline
Одна рекомендация & Метка содержит ровно одну рекомендацию & Выводится эта единственная рекомендация. \\
\hline
Несколько рекомендаций (типичный случай) & Метка содержит от 3 до 5 рекомендаций & Выводятся все рекомендации, их нормализованные оценки в сумме дают 1. \\
\hline
Много рекомендаций (более 10) & Метка содержит более 10 рекомендаций & Отображаются только $n$ лучших рекомендаций по убыванию интегральной оценки, остальные не показываются. \\
\hline
\multicolumn{3}{|c|}{\textbf{Весовая матрица МАИ}} \\
\hline
Согласованная матрица & Матрица попарных сравнений, для которой отношение согласованности CR < 0.1 & Веса вычислены, предупреждение не выводится. \\
\hline
Несогласованная матрица & Матрица с противоречивыми сравнениями, CR > 0.1 & Выведено предупреждение о несогласованности, веса тем не менее рассчитаны. \\
\hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Тестирование по классам эквивалентности часть 3}
\label{tab:tests3}
\begin{tabular}{|p{0.25\textwidth}|p{0.35\textwidth}|p{0.30\textwidth}|}
\hline
\textbf{Класс эквивалентности} & \textbf{Характеристика входных данных} & \textbf{Ожидаемый результат} \\
\hline
\multicolumn{3}{|c|}{\textbf{Аварийные сценарии}} \\
\hline
Сценарий доступен по меткам & Отказ от выполнения действия, чьи метки пересекаются с тегами хотя бы одного аварийного сценария & Пользователю предложен соответствующий аварийный сценарий. \\
\hline
Сценарий недоступен & Отказ от действия, метки которого не пересекаются с тегами ни одного аварийного сценария & Выводится сообщение «Нет доступных аварийных сценариев». \\
\hline
Рекурсивный вызов & Внутри выполнения аварийного сценария пользователь снова отказывается от действия и выбирает другой аварийный сценарий & Загружается вложенный аварийный граф, глубина рекурсии увеличивается (до заданного максимума). \\
\hline
\end{tabular}
\end{table}
% TODO: кто такой эффективность
\section{Оценка вычислительной сложности и потребляемой памяти}

Вычислительная сложность одного шага определяется:
% TODO: преамбула к пунктам
\begin{itemize}
    \item сложностью определения доступных действий --- \(O(|A|)\), где \(|A|\) --- общее число действий;
    \item сложностью подсчета приоритетов для каждого доступного действия --- построение списка рекомендаций: \(O(m \cdot k)\), \(m\) --- число рекомендаций, \(k\) --- число критериев;
    \item сложностью ранжирования --- сортировка \(O(m \log m)\).
\end{itemize}

% TODO: формула под вопросом
Потребление памяти определяется хранением графа и кэша меток: \(O(|G| + |C| + |L| \cdot R)\), где $G$ --- информация о графе, $C$ --- количество действий, \(R\) --- среднее число рекомендаций на метку, $L$ --- размер кэша меток.

\section*{Выводы}

Разработанный метод поддержки принятия решений для пошаговых инструкций использует систему меток и метод анализа иерархий. 
Он позволяет автоматически формировать контекстно-зависимые рекомендации, позволяет экспертам настраивать метод в режиме отладки, а также позволяет обрабатывать внештатные ситуации с помощью заранее заготовленых экспертами аварийных сценариев. 